import React from 'react';
import {View} from 'react-native';

export const emptyStepNumberComponent = ({
    isFirstStep,
    isLastStep,
    currentStep,
    currentStepNumber,
  }) => (
    <View></View>
  );